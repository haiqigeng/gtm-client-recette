interface:
  display_name: "GTM Client Recette"
  short_description: "Run fast, expert GTM Preview acceptance QA"
  default_prompt: "Use $gtm-client-recette to run claim-driven client-side GTM Preview recette in my existing approved Chromium session. Compile the supplied plan directly and start the first safe vertical slice after one bounded persisted identity handshake. Use the fully expanded Tag Assistant API Call as the normal exact source and a call-time recorder only when needed; keep accumulated Data Layer state, Variables, concerned tag inventory/configuration/effective runtime mapping, and decoded browser requests separate. Reuse a valid current Core load, never make an unnamed clean repeat, compare every destination-applicable planned field across applicable surfaces, continuously inspect weird behavior between interactions, and return immediate then canonical per-event, per-layer statuses before the final validated reports."
