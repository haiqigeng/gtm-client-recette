interface:
  display_name: "GTM Client Recette"
  short_description: "Run fast, expert GTM Preview acceptance QA"
  default_prompt: "Use $gtm-client-recette to run claim-driven client-side GTM Preview recette in my existing approved Chromium session. Compile the supplied plan directly and start the first safe vertical slice after one bounded identity handshake. Treat call-time dataLayer/API Call, Tag Assistant Data Layer state, Variables, concerned tag inventory/configuration/effective runtime mapping, and decoded browser requests as separate authorities; compare every destination-applicable planned field across them. Discover material scenarios just in time, continuously inspect weird behavior between interactions, and return mandatory per-event, per-layer statuses before the final validated reports."
