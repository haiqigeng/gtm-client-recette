interface:
  display_name: "GTM Client Recette"
  short_description: "Run Playwright-first expert GTM Preview acceptance QA"
  default_prompt: "Use $gtm-client-recette to run the fastest trustworthy tracking-plan-led GTM Preview recette in headed managed Edge, reconciling reality, exact API Call, concerned GTM tag mapping/firing/runtime, browser request, and surrounding behavior, with immediate explained per-event layer feedback."
dependencies:
  tools:
    - type: "mcp"
      value: "playwright"
      description: "Microsoft Playwright MCP configured for headed Microsoft Edge with a persistent workspace profile and semantic browser/network/Preview capabilities."
