interface:
  display_name: "GTM Client Recette"
  short_description: "Fixed XLSX-to-Playwright GTM acceptance QA"
  default_prompt: "Use $gtm-client-recette to test this XLSX tracking plan in a headed Playwright MCP GTM Preview session."
dependencies:
  tools:
    - type: "mcp"
      value: "playwright"
      description: "Standalone Playwright MCP that opens and controls the single headed browser used for GTM Preview and the target site."
