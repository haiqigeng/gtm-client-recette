interface:
  display_name: "GTM Preview Recette"
  short_description: "Run exhaustive client-side GTM acceptance QA"
  default_prompt: "Use $gtm-preview-recette to register and test every applicable tracking-plan interaction case in order, prove each website action independently, classify and count every business push, close every applicable raw/resolved/GTM/tag/runtime/browser-send layer with direct evidence, complete safe gated journeys, and deliver immediate per-event verdicts plus a strictly validated XLSX and actionable defect register."
