[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "gtm-client-recette"
version = "13.0.0"
description = "Fixed personal imperfect-XLSX and Playwright MCP GTM acceptance recette"
license = "MIT"
requires-python = ">=3.11"
dependencies = ["openpyxl>=3.1.2", "Pillow>=10"]

[project.optional-dependencies]
dev = ["ruff>=0.12.0", "vulture>=2.14", "radon>=6.0"]

[tool.setuptools]
packages = []

[tool.ruff]
target-version = "py311"
line-length = 100
src = ["scripts"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "B", "UP", "SIM"]

[tool.ruff.lint.per-file-ignores]
"tests/*.py" = ["E402"]
